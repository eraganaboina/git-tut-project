name: demo-2
on :
  push:
env:
  cloud: aws
jobs:
  jobs1:
    runs-on: ubuntu-latest
    env:
      services: s3
    steps:
      - name: jdjn
        run: |
          ${{ secrets.MY_KEY }}
          echo "hello world"
        
      - name: step1
        env:
          level: 3.5 out of 5
        run: echo "i know $cloud in that i know servuices like $services $level ${{vars.project_id}}" 
    
  
