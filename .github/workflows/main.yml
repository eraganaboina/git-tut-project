name: demo action
on:
 push: 
jobs:
 bulid:
  runs-on: ubuntu-latest
  steps:
	 -name: run command
	  run: echo "hello world"
	 -name: run commands 2 
	  run: echo "hi"
	 -user: actions/checkout@v4
  
