name: Demo Input

on:
  workflow_dispatch:
    inputs:
      name:
        description: "Enter your name"
        required: true
        default: "Harsha"

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Print input
        run: echo "Hello ${{ github.event.inputs.name }}"
